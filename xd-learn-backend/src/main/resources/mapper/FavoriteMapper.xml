<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.xdlearnbackend.mapper.FavoriteMapper">
    <select id="selectByUserAndCourse" resultType="com.example.xdlearnbackend.entity.Favorite">
        SELECT id, user_id, course_id, created_time
        FROM favorite
        WHERE user_id = #{userId} AND course_id = #{courseId}
    </select>

    <insert id="insert">
        INSERT INTO favorite (user_id, course_id, created_time)
        VALUES (#{userId}, #{courseId}, NOW())
    </insert>

    <delete id="delete">
        DELETE FROM favorite
        WHERE user_id = #{userId} AND course_id = #{courseId}
    </delete>
</mapper>